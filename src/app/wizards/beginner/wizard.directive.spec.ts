import { WizardDirective } from './wizard.directive';

describe('WizardDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardDirective();
    expect(directive).toBeTruthy();
  });
});
